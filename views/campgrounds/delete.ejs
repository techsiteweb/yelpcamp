<% include ../partials/header %>
<div class="container">
    <div class="row"> <!--Here we add the grid system with "row" class-->
        <div class="form">
            <div class="form-header">
                <h3 class="header-text">Delete: <span><%= foundCampGround.name %>?</span></h3>
            </div>
            <div id="content" class="form-fields">
                <form action="/campgrounds/<%= foundCampGround._id %>?_method=DELETE" method="POST">
                    <div class="form-group">
                        <button class="btn btn-lg btn-primary btn-block">Confirm Delete</button>
                    </div>
                </form>
            <div>
                <a href="/campgrounds">Go Back</a>
            </div>
            </div>
        </div>
    </div>
</div> <!--Container ends here-->
<% include ../partials/footer %>